let number = 10;
console.log(number);

let stringOne = "double quotes";
let stringTwo = 'single quotes'
console.log(stringOne, stringTwo);

let on = true;
let off = false;
console.log(on, off);

let empty = null;
console.log(empty);

let undef = undefined;
console.log(undef);

let correct;
console.log(correct);

//objects used to store many values instead of one single one, denoted by curly braces{}
let frodo = {
    race: 'hobbit',
    rings: 1,
    cloak: true
}

console.log(frodo);
console.log(typeof frodo);

//arrays are containers that hold lists of items, denoted by square brackets[]

let list = ['item1', 'item2','item3']
console.log(list);

let burritos = ['large', 4, true]
console.log(burritos);

console.log(typeof burritos);

//DataType literals: represents a fixed value that we as developers insert into the code (ex y = 10)
//includes strings, numbers, booleans, objects, arrays

//string literal
let car = 'ford';

//number literal
let december = 12;

//boolean literal
let tired = true;

//object literal
let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
};

//array literal

let days = ['monday','tuesday','wednesday','thursday','friday'];


